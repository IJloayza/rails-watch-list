<h1>Create a new list</h1>

      <%= form_for @list do |f| %>
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
      <%= link_to 'Back', lists_path, class: 'btn btn-primary' %>
